# rpc-dotnet-toolkit Examples

This directory contains complete working examples demonstrating various features of RPC .NET Toolkit.

## 📂 Examples

### Coming Soon

- **AspNetCore.Server** - Full ASP.NET Core server with RPC endpoint
- **ConsoleClient** - Simple console application using RpcClient
- **BlazorApp** - Blazor WebAssembly with RPC integration
- **CrossPlatform** - Calling Node.js and PHP servers from .NET

## 🚀 Running Examples

Each example is a self-contained project. To run:

```bash
cd examples/AspNetCore.Server
dotnet run
```

Then in another terminal:

```bash
cd examples/ConsoleClient
dotnet run
```

## 📝 Example Structure

Each example includes:
- **README.md** - Detailed explanation
- **Program.cs** - Main application code
- **.csproj** - Project configuration
- **appsettings.json** (for ASP.NET Core examples)

## 🔗 Related

See also the test projects in `tests/` for more code examples.
